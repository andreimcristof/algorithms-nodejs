// --- Directions
// Given an integer, return an integer that is the reverse
// ordering of numbers.
// --- Examples
//   reverseInt(15) === 51
//   reverseInt(981) === 189
//   reverseInt(500) === 5
//   reverseInt(-15) === -51
//   reverseInt(-90) === -9

function reverseInt(n) {
  // two solutions:
  // solution nr 1, turn the number into a string, and reverse it, parse back to number.
  // solution nr 2,
}

module.exports = reverseInt;
